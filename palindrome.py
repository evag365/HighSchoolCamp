import random
##Have your program return a random response of either: Yes definitely, As I see it, yes, Ask again later, Cannot predict now, Don't count on it, Very doubtful, and 4 other responses.

#Call (invoke) the function like shown below:

#print(shake_ball())

input("What is your question?")

shake_ball = []
return responses.random shake_ball

def shake_ball():
    options = ["yes, definitely", "As I see it","Ask again later ", "I don't think so ","my gut says yes"]
    input("Enter a question")
    return random.choice(options)